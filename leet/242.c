#include <stdio.h>
#include <stdbool.h>
#include <string.h>

#define alphabet 26
bool isAnagram(char *a, char *b) {
  if (strlen(a) != strlen(b) {
    return false;
  }
  
  int hash1[alphabet] = {0};
  int hash2[alphabet] = {0};

}
