#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#define alphabets 26

char*** grpAnagrams(char** strs, int strsSize, int* returnSize, int** returnColumnSizes) {
  for (int i = 0; i < strsSize; i++) {
    int hash[alphabets] = {0};
  }
}
int main() {
}
